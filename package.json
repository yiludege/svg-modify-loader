{
  "name": "svg-modify-loader",
  "version": "1.0.0",
  "description": "svg attr operate loader",
  "main": "index.js",
  "scripts": {
    "build": "webpack --config example/webpack.config.js",
    "debugger": "webpack --config example/webpack.config.js --profile --json > compilation-stats.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "svgson": "4.0.0"
  },
  "devDependencies": {
    "file-loader": "^6.0.0",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
